## Packages
framer-motion | Smooth animations and transitions for list items and loading states
date-fns | Human-readable date formatting for generation history

## Notes
The application will use a dark professional theme by default.
Fonts: 'Outfit' for display, 'Inter' for body, 'JetBrains Mono' for training data and outputs.
Backend integration: expects POST /api/generations to handle the text generation (simulation/actual LLM call).
